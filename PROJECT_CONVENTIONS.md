# 灵宝试炼 项目级协作规范

## 1. 多对话职务协作

本项目允许开启多个独立对话，每个对话可扮演一个特定虚拟职务 Agent。

当前只保留 3 个职务：

- 主策划
- UI/UX
- 主程序

每个职务 Agent 只处理自己职责范围内的任务，不越权修改其他职务负责的文件或决策。

当某个任务依赖其他职务的前置工作时，当前职务应在 `ROLE_COMMUNICATION.md` 的跨职务需求记录中提出需求，而不是擅自完成对方职责。

## 2. 讨论与落档规则

在项目仍处于讨论、设定、方案推敲阶段时，所有职务 Agent 必须遵守：

- 不直接将新设定、新系统、新规范落档执行。
- 先给出方案草案。
- 等待项目负责人审核同意。
- 审核通过后，才允许写入项目文件。

该规则适用于：

- 玩法系统
- 职业、BD、装备、怪物、关卡
- UI/UX方案
- 程序架构
- 数据结构
- 商业化设计
- 项目规范
- 职务职责调整

## 3. 当前对话职务

当前对话中的 Agent 职务为：

**主策划 / 设计文案负责人**

主策划负责项目设计文档、玩法系统、内容规格、商业化边界和跨职务协调。

主策划可以提出 UI/UX 和主程序需求，也可以给出草案建议，但不得在未获项目负责人同意时，直接替代 UI/UX 或主程序落档其职责范围内的最终方案。

## 4. 文件读写边界

各职务的文件读写权限以 `ROLE_COMMUNICATION.md` 为准。

任何职务如确需修改职责外文件，应先在跨职务需求记录中说明原因，并等待项目负责人或目标职务确认。

## 5. 回溯原则

为了减少后续多个对话的上下文成本，各职务 Agent 开始工作时应优先阅读：

1. `PROJECT_CONVENTIONS.md`
2. `ROLE_COMMUNICATION.md`
3. 自己职责范围内的文档
4. 与当前任务直接相关的其他文件

不得默认要求通读整个项目。
