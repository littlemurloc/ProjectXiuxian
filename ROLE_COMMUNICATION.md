# 灵宝试炼 虚拟职务沟通文档

## 1. 职务总表

本项目只保留 3 个虚拟职务：

- 主策划
- UI/UX
- 主程序

其他工作，如数值、运营、商业化、美术需求、埋点方案，不单独设职务，由以上 3 个职务按职责分配处理。

## 2. 主策划

当前对话职务：**主策划 / 设计文案负责人**

### 工作职责

- 核心玩法方向与体验目标
- 职业、BD、装备、怪物、事件、关卡节奏设计
- 数值框架和首测指标
- 商业化边界、付费点、运营目标
- 世界观、命名、文本风格
- 美术需求的设计侧描述
- 设计文档维护
- 跨职务需求协调

### 主要负责文件

- `DESIGN.md`
- `CONTENT_SPEC.md`
- `PROJECT_CONVENTIONS.md`
- `ROLE_COMMUNICATION.md`
- 后续 `docs/design/` 下的设计文档

### 可读取文件

- 全部项目文件

### 写入边界

主策划可以写入设计与规范文档。

主策划不得在未获项目负责人明确同意时，直接修改：

- UI/UX 最终方案文档
- 程序架构文档
- `src/` 下的核心程序实现
- `index.html`
- `styles.css`
- `server.js`
- `smoke-test.js`

如主策划需要 UI/UX 或主程序支持，应在本文档的跨职务需求记录中提出。

## 3. UI/UX

### 工作职责

- 页面信息架构
- 战斗 HUD
- 背包、结算、商店、通行证界面
- 交互流程
- 可读性与操作效率
- 原型视觉规范
- 美术表现需求整理，如图标、品质色、技能反馈、界面动效

### 主要负责文件

- 后续 `docs/ui/` 下的 UI/UX 文档
- UI 原型说明
- 界面流程图
- 样式规范文档

### 可读取文件

- `PROJECT_CONVENTIONS.md`
- `ROLE_COMMUNICATION.md`
- `DESIGN.md`
- `CONTENT_SPEC.md`
- `IMPLEMENTATION_PLAN.md`
- 程序文件中与界面结构直接相关的部分

### 写入边界

UI/UX 可以写入 UI/UX 相关文档。

UI/UX 不得在未获项目负责人明确同意时，直接修改：

- 核心玩法规则
- 职业、BD、装备、怪物、商业化边界
- 程序架构
- `src/` 下的核心程序实现

如 UI/UX 需要玩法设定补充，应向主策划提出需求。  
如 UI/UX 需要技术可行性判断，应向主程序提出需求。

## 4. 主程序

### 工作职责

- Web 原型技术架构
- 游戏循环
- 输入、战斗、碰撞、怪物 AI
- 数据驱动结构
- 装备、掉落、事件和结算的程序实现
- 本地存档与事件日志
- 测试脚本
- 本地服务器
- 将设计和 UI/UX 方案转化为可运行原型

### 主要负责文件

- `index.html`
- `styles.css`
- `server.js`
- `smoke-test.js`
- 后续 `src/` 下的程序文件
- 后续 `docs/technical/` 下的技术文档

### 可读取文件

- `PROJECT_CONVENTIONS.md`
- `ROLE_COMMUNICATION.md`
- `DESIGN.md`
- `CONTENT_SPEC.md`
- `IMPLEMENTATION_PLAN.md`
- UI/UX 相关文档

### 写入边界

主程序可以写入程序文件、测试文件和技术文档。

主程序不得在未获项目负责人明确同意时，直接修改：

- 核心玩法方向
- 职业、BD、装备、怪物、事件的设计定义
- 商业化边界
- UI/UX 最终方案

如主程序因设计缺口无法实现，应向主策划提出需求。  
如主程序因界面流程不清无法实现，应向 UI/UX 提出需求。

## 5. 跨职务沟通规则

当某职务 Agent 遇到阻塞时，应在本文档的“跨职务需求记录”中追加需求，不得擅自越权处理。

需求格式：

```md
### YYYY-MM-DD 发起职务 -> 目标职务

需求标题：

背景：

需要对方提供：

阻塞影响：

期望输出文件或结论：

状态：待处理 / 处理中 / 已完成
```

## 6. 跨职务需求记录

### 2026-05-25 主策划 -> 主程序

需求标题：网页版原型流程拆分、机制表现补全与鼠标移动输入

背景：

主程序已完成 5 个切片后的网页版本，但体验反馈显示：当前功能仍偏集中在一页；部分职业、法宝、升级和掉落机制缺少最低画面表达；敌人只有颜色球体难以识别；场景资源掉落缺少可见反馈；移动方式只支持键盘，不足以覆盖鼠标操作习惯。

需要对方提供：

1. 页面流程调整：
   - 默认进入“试炼准备页”，包含选职业、选层级、选节奏、选法宝、查看装备、开始试炼。
   - 点击开始后进入独立“局内战斗页”，战斗中不展示完整商店和复杂局外养成面板。
   - 结算后可进入再来一局、整理装备、洗练装备、升级法宝或返回准备页。
   - 局外养成页与商店/验证页和战斗页分离。

2. 输入补充：
   - 保留 WASD/方向键移动。
   - 增加鼠标左键按住拖动移动：按住时玩家朝鼠标当前位置持续移动，松开左键停止鼠标移动。
   - 鼠标移动时显示目标点或方向线，接近目标点后停止，避免抖动。

3. 画面表现补全：
   - 每个敌人头顶显示名称；精英/Boss额外显示血条和身份边框。
   - 灵气、血珠、法宝灵息、灵石小掉落必须在场景中显示不同图标，并有拾取反馈。
   - 职业机制需要最低表现：飞剑穿透、暴击剑影、回锋、剑痕留场、连锁雷击、雷池、天罚、麻痹、雷暴。
   - 法宝机制需要最低表现：6 个法宝都要有触发反馈、冷却状态和对应场景效果。
   - 敌人行为需要最低表现：快怪拖尾、咒鸦弹体、爆灵倒计时圈、冲锋/Boss技能预警区。
   - 玩家状态需要最低表现：受击、低血、回血、护盾。

4. 机制自检：
   - 数据表中已配置的职业、法宝、升级、敌人和掉落，必须至少有一种局内画面表达。
   - 如果机制只存在日志或数值变化，画面看不出来，视为表现未完成。
   - 如果某个设计机制尚未实现，请在本文档追加“程序待补”说明，不要默默省略。

阻塞影响：

如果不补以上内容，玩家无法判断机制是否真实生效，刷宝反馈和战斗可读性不足，后续试玩反馈会混淆“玩法不好玩”和“表现看不懂”两个问题。

期望输出文件或结论：

- 更新 `index.html` / `styles.css` / `game.js` / `src/` 相关实现。
- 如涉及结构调整，可同步更新 `IMPLEMENTATION_PLAN.md` 的技术备注，但不得改动核心设计规则。
- 完成后运行 `npm test`，并启动本地服务供浏览器验证。

状态：已完成

主程序进展：

- 已完成第一批可运行实现：流程拆分为试炼准备、局内战斗、局外养成、商店验证；开始试炼后进入战斗页，结算后可回到整理装备、法宝养成、职业成长或准备页。
- 已补充鼠标左键按住/拖动移动，保留 WASD/方向键；鼠标移动时显示目标点与方向线，靠近目标后停止。
- 已补充敌人名称、精英/Boss边框与血条、快怪拖尾、咒鸦弹体、爆灵倒计时圈、冲锋/Boss预警区、资源掉落图标、拾取飘字、玩家受击/低血/回血/护盾反馈。

主程序补充进展：

- 已补齐职业与升级机制的专属表现：暴击剑影、追影、回锋、剑痕留场、连斩、连锁雷击、雷池、天罚、麻痹、雷暴均有局内画面反馈。
- 已补齐 6 个法宝的场景触发反馈：青锋匣、引雷珠、回春葫芦、玄冰镜、聚妖铃、炼火珠均有独立脉冲/区域/目标表现，不再只依赖日志或冷却数字。

程序待补：

- 暂无主策划本条需求下的阻塞残项。后续若 UI/UX 要进一步提高辨识度，可继续拆分为图标、音效占位、技能色彩规范和特效层级规范。

### 2026-05-25 主程序 -> 主策划

需求标题：局内奖励暂停与弹窗串联机制是否需要补入设计文档

背景：

根据试玩体验反馈，主程序已对局内奖励弹窗流程做了程序侧调整：装备获取触发装备对比弹窗时会暂停战斗；局内升级 3 选 1 与装备更换弹窗进入同一个奖励队列；同一时间只展示一个待处理弹窗，玩家完成当前窗口操作后，才会继续处理下一个奖励窗口；队列清空后恢复战斗逻辑。

需要对方提供：

- 判断该规则是否应作为正式局内流程规则写入 `DESIGN.md` 或 `CONTENT_SPEC.md`。
- 若需要写入，请明确文档表述口径：例如“局内奖励结算队列”“所有强决策弹窗暂停战斗”“升级与装备选择按触发顺序串联处理”等。
- 判断是否还需要覆盖更多强决策弹窗类型，例如风险事件、法宝选择、临时商店或 Boss 奖励。

阻塞影响：

当前程序已按上述规则实现并通过测试；不阻塞继续开发。但如果设计文档不记录该规则，后续 UI/UX 或玩法扩展可能误以为装备弹窗可以与升级弹窗并行，导致流程标准不一致。

期望输出文件或结论：

- 主策划确认是否需要更新设计文档。
- 如需更新，请给出应写入的目标文档与规则描述。

状态：已完成

主策划回复：

- 需要更新设计文档。该规则属于正式局内流程规则，不只是程序实现细节。
- 已在 `DESIGN.md` 增加“局内强决策奖励队列”规则。
- 已在 `CONTENT_SPEC.md` 增加“局内强决策奖励队列”规格，明确入队/不入队类型。
- 已在 `IMPLEMENTATION_PLAN.md` 增加程序验收：升级三选一、装备对比、风险事件、精英/Boss奖励进入同一队列；同一时间只展示一个强决策弹窗；当前弹窗完成后再处理下一个，队列清空后恢复战斗。

设计口径：

- 入队类型：升级三选一、同部位装备对比、风险事件选择、精英/Boss后的稀有升级或关键奖励选择。
- 不入队类型：普通资源拾取、普通日志、伤害/回血/护盾浮字、法宝触发提示、商店入口提示。
- 目的：避免奖励窗口堆叠、误操作和处理装备/升级时被怪物击杀，保证高价值选择被玩家看懂。

### 2026-05-25 主策划 -> 主程序

需求标题：装备对比弹窗频率控制与主动查看候选装备

背景：

试玩反馈显示，中后期怪物掉落装备频率偏高，虽然“同部位不自动覆盖 + 装备对比弹窗”的机制正确，但装备替换弹窗过于频繁，严重打断幸存者式战斗节奏。主策划已调整设计口径：保留装备替换机制，但装备对比弹窗需要节奏控制。

需要对方提供：

1. 装备弹窗软上限：
   - 0:00-2:00：普通装备对比弹窗最多 1 次。
   - 2:00-5:00：普通装备对比弹窗最多 2 次。
   - 5:00-8:00：普通装备对比弹窗最多 2 次。

2. 高价值例外：
   - 超过当前时间段软上限后，如果新装备评分高于当前同部位装备 15 分以上，仍允许弹窗。
   - 超过当前时间段软上限后，如果新装备为稀有及以上，且适配当前职业、主流派或携带法宝，仍允许弹窗。

3. 全局绝对上限：
   - 每局强制装备对比弹窗最多 5 次。
   - 达到全局上限后，不再强制弹出装备对比。
   - 高价值装备进入候选栏，并用 HUD 提示“发现高价值装备，按 E 查看”。
   - 玩家主动按 E 或点击候选栏查看装备不计入弹窗上限。

4. 低价值装备处理：
   - 超过时间段软上限且不满足高价值例外的装备，不触发弹窗。
   - 首版可将这类低价值装备转化为少量灵石，或直接不生成装备。
   - 处理时不要让玩家误以为高价值装备被吞掉。

阻塞影响：

如果不控制装备弹窗频率，中后期战斗会被低价值装备反复切断，影响节奏判断和乐趣验证。

期望输出文件或结论：

- 更新装备掉落和装备对比弹窗触发逻辑。
- 更新 HUD 或候选栏提示，支持高价值装备主动查看。
- 完成后运行 `npm test` 并进行一局中后期掉落压力自测。

状态：已完成

主程序进展：

- 已实现装备对比弹窗频率控制：0:00-2:00 普通装备最多强制弹 1 次，2:00-5:00 最多 2 次，5:00-8:00 最多 2 次；每局强制装备对比弹窗全局最多 5 次。
- 已实现高价值例外：评分高于当前同部位 15 分以上，或高评分稀有候选且适配当前职业、主流派、携带法宝时，可在阶段软上限后继续强制弹窗；全局上限达到后不再强制弹窗。
- 已实现主动查看：高价值装备在全局上限后进入候选栏，并显示 HUD 提示“发现高价值装备，按 E 查看”；玩家按 E 或点击候选栏可主动打开装备对比，不消耗强制弹窗次数。
- 已实现低价值处理：超过阶段软上限且不满足高价值例外的装备，转化为少量灵石并给出掉落日志/飘字反馈，避免玩家误以为高价值装备被吞。

验证：

- `npm test` 已通过。
- 已通过本地服务检查确认 `game.js` 可加载新逻辑。

### 2026-05-25 主策划 -> 主程序

需求标题：职业爽感、法宝存在感与装备提升感第一轮强化

背景：

试玩反馈显示，当前版本战斗流程和装备弹窗节奏基本可接受，但职业爽感、法宝存在感、装备提升感仍偏弱。主策划已确认采用“成型爆点和机制装备为主、表现强化必做、数值小幅辅助”的改善方向。

需要对方提供：

1. 职业成型爆点：
   - 剑修暴击成型：暴击击杀后，3 秒内下一次飞剑必定生成剑影。
   - 剑修穿透成型：飞剑每穿透 3 个敌人后，末端释放一次小范围剑气。
   - 雷修雷击成型：雷击最后一跳必定造成小范围爆炸。
   - 雷修雷池成型：雷池持续时间 +1 秒，场上最多同时存在 2 个。
   - 剑修/雷修 + 灵宝：法宝触发后 2 秒内，剑修飞剑穿透 +1；雷修下一次雷击额外跳跃 +1。

2. 法宝存在感：
   - 冷却下调：青锋匣 5 秒、引雷珠 5.5 秒、玄冰镜 8 秒、炼火珠 7 秒、聚妖铃 16 秒、回春葫芦 22 秒。
   - 强化触发表现：青锋匣剑匣虚影、引雷珠雷纹扩散、回春葫芦绿色护体圈、玄冰镜冰域描边、聚妖铃收益圈倒计时、炼火珠预警火印。

3. 6 件核心装备机制化：
   - 青锋剑：飞剑基础穿透 +1；每第 5 次飞剑变为宽剑气。
   - 引雷杖：雷击跳跃 +1；最后一跳造成微型电弧。
   - 剑心戒：暴击时额外生成 1 道小剑影，内置冷却 1 秒。
   - 雷纹戒：麻痹敌人受到伤害提高；雷击优先跳向麻痹目标。
   - 聚灵符：法宝冷却 -15%；拾取法宝灵息时额外缩短法宝冷却。
   - 灵宝玉：自动法宝触发后，下次职业普攻强化。

4. 装备与结算反馈：
   - 替换装备后 HUD 显示新增机制提示，例如“获得宽剑气”“法宝冷却 -15%”。
   - 装备卡增加“机制”字段，不只显示数值。
   - 结算页新增“本局变强来源”，展示职业升级、法宝、装备、流派成型带来的关键机制。若暂时无法精确统计输出占比，先展示机制来源列表。

阻塞影响：

如果不强化以上内容，玩家会感觉职业、法宝和装备都只是小数值叠加，刷宝BD的复玩动力不足。

期望输出文件或结论：

- 更新 `src/data.js`、`game.js`、必要的 UI 渲染与样式。
- 完成后运行 `npm test`。
- 建议用 4 分钟快测模式分别体验剑修和雷修，确认至少能明显感到一次职业爆点、一次法宝触发高潮和一次装备机制变化。

状态：已完成

主程序进展：

- 已实现职业成型爆点：剑修暴击击杀后的必定剑影、穿透累计 3 次后的范围剑气；雷修最后一跳爆炸、雷池持续时间延长并限制最多 2 个；法宝触发后的职业短时强化已接入。
- 已强化法宝存在感：6 个法宝冷却已按新口径下调，并保留/强化各自的场景脉冲、范围圈、收益圈、火印/爆发等表现。
- 已机制化 6 件核心装备：青锋剑、引雷杖、剑心戒、雷纹戒、聚灵符、灵宝玉均增加 `mechanic` 字段，并接入对应战斗触发逻辑。
- 已补装备与结算反馈：装备获得/替换时提示新增机制；装备对比卡展示机制字段；结算页新增“本局变强来源”，展示职业、法宝、装备和流派爆点来源。

验证：

- `npm test` 已通过。
- 本地服务已重启并确认 `/health` 返回 ok。
- 已确认 `game.js` / `src/data.js` 可从 `http://127.0.0.1:4173/` 加载到本轮新增逻辑。

### 2026-05-25 主策划 -> 主程序

需求标题：升级池扩容、法宝技表达与装备技表达第二轮强化

背景：

试玩反馈显示，当前版本职业大致可接受，但法宝和装备感觉仍弱；同时职业成长选项偏少，玩家选择几个升级后容易感觉池子见底。主策划判断主要问题不是单纯数值不足，而是升级池结构过窄，以及法宝/装备没有被包装成清晰的“技能来源”。本轮设计已审核通过，采用“升级池分层扩容 + 法宝技 + 装备技”的推荐方案。

需要对方提供：

1. 升级池扩容：
   - 将局内升级分为基础强化、机制升级、成型进阶三类。
   - 基础强化支持 I/II/III 三级重复选择，满级后移出池子。
   - 机制升级和成型进阶不可重复。
   - 同一流派标签累计 3 次后，解锁对应成型进阶。
   - 三选一每次至少有 1 个当前职业相关选项。
   - 玩家某标签已有 2 次后，提高同标签后续出现权重。
   - 可选池不足时优先补通用基础强化，不能出现升级界面无牌可选。
   - 首批按约 42 个升级项实现：职业基础强化 12、通用基础强化 12、机制升级 10、成型进阶 8。

2. 法宝技表达：
   - 每个法宝增加技能名字段，并在 HUD、触发提示、场景表现和结算页中展示。
   - 触发提示格式建议：`青锋匣：匣中飞剑`、`引雷珠：珠引天雷`。
   - 法宝触发必须有明显场景表现，不能只依赖日志或数值变化。
   - 结算页展示法宝触发次数；如可行，展示伤害、治疗、控制或影响敌人数。

3. 装备技表达：
   - 核心装备增加装备技名称字段。
   - 装备卡展示装备技名称、触发条件和效果。
   - 装备替换弹窗除评分外，必须对比装备技。
   - 触发提示格式建议：`青锋剑：宽剑气`、`引雷杖：余雷电弧`。
   - 结算页展示装备技触发次数；如可行，展示额外伤害、额外命中、冷却减少或强化攻击贡献。

4. 提示密度：
   - 同一时间最多显示 1 条法宝技提示 + 2 条装备技小提示。
   - 同一装备技 3 秒内重复触发时，可刷新计数，不重复弹完整文字。
   - 法宝技提示优先级高于装备技提示。

阻塞影响：

如果不做本轮补充，玩家会继续把法宝和装备理解成后台小数值；升级池也会在中后期枯竭，影响 8 分钟一局的持续成长节奏。

期望输出文件或结论：

- 更新 `src/data.js` 的升级、法宝、装备字段。
- 更新 `game.js` 中升级抽取、等级堆叠、成型进阶解锁、法宝技/装备技触发提示与统计。
- 必要时更新 `styles.css` 和界面渲染，让卡片、HUD、结算页能展示法宝技和装备技。
- 完成后运行 `npm test`。
- 建议分别用剑修和雷修跑 4 分钟快测，确认不会升级池见底，并能明显看到法宝技和装备技触发。

状态：已完成

主程序进展：

- 已完成升级池第二轮扩容：当前 `src/data.js` 配置 42 个局内升级项，包含基础强化、机制升级、成型进阶三类；基础强化支持 I/II/III 三级重复选择，满级后移出池子；机制升级与成型进阶不可重复。
- 已更新升级抽取逻辑：三选一会优先保证当前职业相关选项；同标签累计选择会提高后续权重；成型进阶需要对应标签累计 3 次后进入可选池；升级选项展示时重新抽取，避免队列旧牌导致重复或空牌。
- 已补法宝技表达：6 个法宝均增加 `skillName`，HUD 法宝栏、触发提示、场景脉冲和结算统计会展示法宝技。
- 已补装备技表达：6 件核心装备增加 `skillName` / `triggerText`，装备卡、装备替换弹窗和触发提示会展示装备技；青锋剑、引雷杖、剑心戒、雷纹戒、聚灵符、灵宝玉均接入触发统计。
- 已补提示密度与统计：同屏最多 1 条法宝技提示 + 2 条装备技提示；同装备技短时间重复触发会刷新计数；结算页展示法宝技/装备技触发次数。

验证：

- `npm test` 已通过。
- 本地服务 `/health` 返回 ok。
- 数据检查结果：升级项 42 个，基础强化 19 个，机制升级 14 个，成型进阶 9 个；6 个法宝均有法宝技，6 件核心装备均有装备技。

### 2026-05-26 主策划 -> 主程序

需求标题：长期复玩第一阶段：灵契联动系统

背景：

当前版本单局耐玩性已基本达到预期，但长期反复挑战存在新风险：同一个职业每局的体验和成长路线仍容易趋同。主策划判断不应优先靠新增大量职业或起手分支堆内容，而应先让已有职业、法宝、装备技和升级标签产生组合联动。灵契系统已审核通过，作为长期复玩改造第一阶段。

需要对方提供：

1. 数据结构：
   - 新增灵契配置表，建议命名为 `SYNERGIES` 或等价结构。
   - 每条灵契包含 `id`、名称、层级、条件、效果、提示文案、统计字段。
   - 层级分为轻联动、核心联动、变质联动。
   - 条件需要能识别职业、法宝、装备、升级标签、特定升级。
   - 支持检测“已激活”和“差 1 条件”状态。

2. 首版灵契数量：
   - 12 个轻联动。
   - 6 个核心联动。
   - 3 个变质联动。
   - 具体清单以 `CONTENT_SPEC.md` 的“灵契联动规格”为准。

3. 触发逻辑：
   - 条件满足后自动激活。
   - 激活后本局持续生效，结算后清空。
   - 首次激活时显示 `灵契觉醒：xxx`。
   - 灵契效果不得造成递归无限触发。
   - 变质联动可以是短时高光，不要求长期常驻。

4. UI 与反馈：
   - 构筑面板显示已激活灵契。
   - 构筑面板或升级界面显示接近完成的灵契和缺少条件。
   - 升级卡如果选择后可激活或接近灵契，显示角标。
   - 装备替换弹窗显示可激活灵契、强化已有灵契、替换后失去灵契。
   - 结算页显示本局激活灵契和触发次数/贡献；统计不完整时先显示机制来源列表。

5. 装备推荐评分补充：
   - 激活轻联动：推荐分 +8。
   - 激活核心联动：推荐分 +15。
   - 激活变质联动：推荐分 +25。
   - 替换后失去核心/变质联动：给出明显警告。

阻塞影响：

如果不实现灵契系统，后续长期复玩仍主要依赖新增职业、装备和升级内容量，容易变成“1 天玩腻和 2 天玩腻”的区别。灵契的目标是把复玩动力从内容堆叠转向组合深度。

期望输出文件或结论：

- 更新 `src/data.js` 的灵契配置和相关字段。
- 更新 `game.js` 中条件检测、激活、效果应用、提示、统计和结算逻辑。
- 必要时更新 `styles.css` 和 UI 渲染，让构筑面板、升级卡、装备替换弹窗、结算页能展示灵契。
- 更新 `smoke-test.js`，检查灵契配置数量和基础字段完整性。
- 完成后运行 `npm test`。
- 建议用 4 分钟快测和 8 分钟正式节奏分别体验，确认自然联动、追核心联动和变质联动表现。

状态：已完成

主程序进展：

- 已新增 `SYNERGIES` 灵契配置表，首版共 21 条：12 个轻联动、6 个核心联动、3 个变质联动；每条包含 `id`、名称、层级、条件、效果、统计字段。
- 已实现灵契条件检测：支持职业、法宝、装备、升级标签、特定升级和法宝灵息等条件；支持“已激活”和“差 1 条件”状态。
- 已实现自动激活：满足条件后本局自动激活，首次显示 `灵契觉醒：xxx`，并写入本局变强来源；结算后随本局状态清空。
- 已接入 UI：构筑面板展示已激活灵契与接近完成灵契；升级卡展示可激活/接近灵契角标；装备替换推荐文本会提示可激活灵契或失去核心/变质灵契。
- 已接入装备推荐评分：新装备可激活轻/核心/变质灵契分别增加推荐分；替换后失去核心/变质灵契会给出警告。
- 已接入结算页：展示本局激活灵契及其效果，统计不完整时以机制来源列表方式复述。

验证：

- `npm test` 已通过。
- 本地服务 `/health` 返回 ok。
- 数据检查结果：灵契 21 条，轻联动 12 条，核心联动 6 条，变质联动 3 条；`game.js` 已加载灵契检测、装备推荐影响和灵契觉醒提示逻辑。

### 2026-05-26 主策划 -> 主程序

需求标题：灵契体感强化：核心/变质联动真实机制与装备弹窗强化

背景：

试玩反馈显示，灵契系统的“差 1 条件”提示有效，变质联动方向也有记忆点，但整体体感仍偏一般，装备替换时灵契影响暂时不明显。主策划检查后判断：当前版本已经完成灵契规则骨架，但不少灵契效果仍主要表现为属性加成、提示和结算复述，尚未充分变成可见战斗行为。

本轮目标不是新增灵契数量，而是把已有 21 条灵契做出战斗体感。优先级为：6 个核心联动独立机制、3 个变质联动高光表现、装备替换弹窗灵契区域、结算页触发统计，最后再补轻联动小反馈。

需要对方提供：

1. 核心联动真实机制：
   - 青锋追影：青锋匣飞剑暴击时也生成小剑影，小剑影颜色区别于职业剑影，首次触发显示“青锋追影”。
   - 雷域连锁：冰域内雷击额外分裂一条短电弧，命中时显示“雷域连锁”。
   - 铃火炼妖：聚妖铃结束时，在中心触发一次小爆炎，提示“铃火炼妖”。
   - 血葫护体：回春葫芦治疗溢出转化为护盾，显示“血葫护体”。
   - 灵宝灌剑：法宝触发后的下一次宽剑气额外变宽，颜色变金青。
   - 灵宝灌雷：法宝触发后的下一次余雷电弧额外分裂，提示“灵宝灌雷”。

2. 变质联动高光：
   - 万剑开匣：青锋匣触发后，下一次飞剑攻击变成 1.5 秒短暂剑雨，显示大提示“万剑开匣”。
   - 九霄雷狱：雷爆后留下短暂雷痕区域，区域内敌人持续麻痹，显示“九霄雷狱”。
   - 血炼宝身：护盾存在时，法宝触发强化下一次职业攻击并附带吸血，显示“血炼宝身”。

3. 装备替换弹窗强化：
   - 新增独立“灵契影响”区域。
   - 显示可激活、接近、替换后失去的灵契。
   - 激活核心/变质灵契时强高亮。
   - 替换后失去核心/变质灵契时给明显警告。

4. 结算页强化：
   - 灵契结算不要只展示名称和效果。
   - 至少展示触发次数、生成次数、护盾次数、冷却减少次数、影响敌人数或清场窗口次数中的一种。
   - 变质联动必须单独展示触发次数。

5. 轻联动小反馈：
   - 可排在最后补。
   - 轻联动允许保留数值增强，但至少要有小浮字、图标脉冲或局部特效。

阻塞影响：

如果只保留当前骨架，玩家会看到灵契提示，但不一定感到战斗行为真的变化；长期复玩仍难从“提示新鲜”变成“组合深度”。本轮完成后，灵契第一阶段才可视为战斗体感完成。

期望输出文件或结论：

- 更新 `game.js` 中核心/变质灵契实际战斗逻辑、触发统计和表现。
- 必要时更新 `styles.css`、UI 渲染和结算页。
- 如需补充配置字段，可更新 `src/data.js`。
- 完成后运行 `npm test`。
- 建议分别测试剑修青锋路线、雷修雷域路线、回春吸血路线，确认核心/变质灵契在战斗中可见。

状态：已完成

主程序进展：

- 已补齐 6 个核心灵契的可见战斗行为：青锋匣暴击飞剑生成青色追影、雷域内雷击分裂短电弧、聚妖铃结束中心爆炎、回春葫芦溢出治疗转护盾、灵宝灌剑金青宽剑气、灵宝灌雷额外分裂电弧。
- 已补齐 3 个变质灵契高光：万剑开匣触发 1.5 秒剑雨、九霄雷狱在雷暴后留下麻痹雷痕区域、血炼宝身在有护盾时强化下一次职业攻击并附带吸血。
- 已强化装备替换弹窗，新增独立“灵契影响”区域，分别展示可激活、接近、替换后失去的灵契；核心/变质激活高亮，失去核心/变质时给警告。
- 已强化结算页灵契统计，普通灵契展示触发次数，变质联动独立展示高光触发次数。
- 轻联动保持数值增强，并沿用激活时浮字与灵契脉冲反馈。

验证：

- `npm test` 已通过。
- 本地服务 `/health` 返回 ok，`/game.js` 返回 200。

### 2026-05-26 主程序 -> 主策划

沟通主题：灵机锁定升级交互细则是否需要补入设计案

背景：

试玩反馈显示，升级三选一中每个选项显示 `锁定 -1 灵机` 时，玩家容易误解为“锁定后本次仍可直接选择该项”，或者误以为每个选项都需要额外付费/扣资源。主程序侧已按项目负责人确认做了交互明确。

程序侧已调整：

- 升级弹窗顶部增加说明：`可花费 1 灵机锁定一个未选择升级，下次升级必出`。
- 当玩家锁定某个升级后，该升级卡片本次进入锁定态，显示 `已锁定，下次升级出现`。
- 被锁定升级本次不能再直接选择，玩家需要从其他未锁定选项中选择一个继续。
- 下次升级时，锁定项占据一个槽位保底出现。

规则影响判断：

- 不改变灵机系统核心规则：仍是消耗 1 灵机锁定、仍是一局同时最多 1 个锁定项、仍是下次升级保底出现。
- 新增/明确的是交互边界：锁定后的升级视为“未选择但已预约”，本次不可再选，避免玩家花费灵机后又直接选中同一项造成资源浪费和理解混乱。

需要主策划判断：

- 是否需要把“锁定后本次不可再选择该升级，下次升级必出”补入灵机系统设计案。
- 是否需要在 UI 文案规范中保留当前顶部提示，作为灵机锁定功能的标准说明。

状态：待主策划确认

主策划回复：

- 确认需要补入设计文档。该内容属于灵机锁定功能的交互边界，不只是 UI 文案。
- 设计口径：锁定升级后，该升级视为“已预约但未选择”，本次升级界面不可再选择该项；它将在下次升级三选一中占据一个槽位并必定出现。
- 若下次升级时条件不再满足，则返还 1 灵机，或替换为同标签可用升级。
- 升级弹窗顶部说明固定为：`可花费 1 灵机锁定一个未选择升级，下次升级必出`。
- 已同步更新 `DESIGN.md`、`CONTENT_SPEC.md`、`IMPLEMENTATION_PLAN.md`。

状态：已完成

### 2026-05-26 主策划 -> 主程序

需求标题：长期复玩第三阶段：灵机系统

背景：

灵契系统和邪契/劫契系统已完成首版并经试玩确认有效。第三阶段需要加入局内临时决策资源“灵机”，让玩家在随机升级、掉落、契约不顺时，能有限度地主动修正本局构筑路线。灵机只服务本局构筑调整，不带出局，不接商业化。

需要对方提供：

1. 灵机资源获取：
   - 第一精英 +1 灵机。
   - 风险事件 +1 灵机。
   - 小Boss +1 灵机。
   - 第二精英 +1 灵机。
   - 普通怪不掉灵机。
   - 结算后灵机清空。
   - 目标一局平均 3-4 点，理论上限约 5 点。

2. 刷新升级：
   - 升级三选一界面增加 `刷新 -1 灵机`。
   - 如果本局还有免费刷新，优先消耗免费刷新。
   - 消耗灵机后重新抽取 3 个升级。
   - 避免刚刷掉的一组原样再次出现。
   - 当前有接近灵契时，刷新池轻微提高相关标签权重。
   - 灵机不足时按钮置灰。

3. 锁定升级：
   - 每次升级界面最多锁定 1 个未选择升级。
   - 锁定消耗 1 灵机。
   - 锁定项进入“下次升级保留槽”。
   - 下次升级三选一中，锁定项占据一个槽位。
   - 一局同时最多存在 1 个锁定项。
   - 条件不再满足时，返还 1 灵机，或替换为同标签可用升级。
   - 已选择升级不能锁定。
   - 已满级基础强化不能锁定。

4. 灵匣：
   - HUD 提供主动入口，消耗 2 灵机开启。
   - 灵机达到 2 时提示 `灵机充足，可开启灵匣`。
   - 灵匣不强制弹窗。
   - 开启后进入强决策奖励队列，暂停战斗。
   - 一局最多开启 1 次。
   - Boss 结算后不能开启。
   - 灵机不足时入口置灰。
   - 首版灵匣奖励池：4 个稀有升级、4 个邪契、4 个高适配装备候选、4 个法宝强化。

5. UI 与结算：
   - HUD 显示 `灵机 x`。
   - 悬停说明：本局临时资源，可用于刷新升级、锁定升级或开启灵匣。结算后清空。
   - 升级界面显示刷新和锁定按钮。
   - 当前有锁定项时显示 `已锁定：xxx，下次升级出现`。
   - 结算页显示：获得灵机、刷新次数、锁定次数、开启灵匣次数、未使用灵机。

6. 商业化边界：
   - 不卖灵机。
   - 不卖额外灵机。
   - 不卖灵匣次数。
   - 不卖锁定次数。
   - 不卖刷新成功率。
   - 月卡/礼包不提供灵机。
   - 灵机不带出局，不进入局外资源系统。

阻塞影响：

如果没有灵机，灵契和契约虽然已经提供组合与风险，但玩家在随机不给关键件时仍只能被动接受。灵机用于提供有限修正权，验证玩家是否会主动追灵契、核心升级或契约。

期望输出文件或结论：

- 更新 `game.js` 中灵机获取、消耗、刷新、锁定、灵匣和结算统计逻辑。
- 必要时更新 `src/data.js` 的灵匣奖励池配置。
- 更新 HUD、升级弹窗、灵匣弹窗和结算页 UI。
- 更新 `smoke-test.js` 检查灵机相关 UI/状态文本。
- 完成后运行 `npm test`。
- 建议测试追灵契、锁关键升级、强局开灵匣三种场景。

状态：已完成

主程序进展：

- 已新增本局临时资源“灵机”：第一精英、风险事件、小Boss、第二精英等节点可获得；结算后清空，不进入局外资源。
- 已在 HUD 增加 `灵机` 显示和 `开启灵匣` 主动入口；灵机不足、已开启、暂停中或结算后会禁用入口。
- 已改造升级弹窗：显示本局免费刷新/`刷新 -1 灵机`；免费刷新优先消耗，之后才消耗灵机；刷新会尽量避免原三选一原样再出，并根据接近灵契的缺口标签提高相关权重。
- 已实现升级锁定：每次升级最多锁定 1 个未选择升级，消耗 1 灵机；下次升级进入保留槽；条件不满足时会尝试替换同标签升级，否则返还 1 灵机。
- 已实现灵匣：消耗 2 灵机主动开启，一局最多 1 次；开启后进入奖励队列暂停战斗；首版奖励包含稀有升级、邪契、高适配装备候选、法宝强化四类。
- 已在构筑面板显示当前灵机、锁定项和灵匣状态；结算页展示获得灵机、刷新次数、锁定次数、开启灵匣次数和未使用灵机。
- 已更新 `smoke-test.js`，检查灵机 HUD、锁定提示、灵匣弹窗、奖励池配置和核心实现标记。

验证：

- `npm test` 已通过。
- 本地服务 `/health` 返回 ok，`/game.js` 返回 200。

### 2026-05-26 主策划 -> 主程序

需求标题：长期复玩第二阶段：邪契/劫契系统

背景：

灵契第一阶段已完成并经试玩确认 OK。下一步进入长期复玩第二阶段：邪契/劫契。该系统不用于堆新内容，而是引入“强收益 + 明确代价”的局内风险决策，让玩家根据当前职业、法宝、装备、灵契和血量判断是否接受高风险奖励。

需要对方提供：

1. 数据结构：
   - 新增契约配置表，建议命名为 `PACT_OPTIONS` 或等价结构。
   - 每条契约包含 `id`、类型、名称、收益文案、代价文案、适合局势、效果、代价效果、出现入口、统计字段。
   - 类型分为邪契和劫契。
   - 契约只在本局生效，结算后清空。

2. 首版契约范围：
   - 邪契 6 个：断脉剑诀、燃魂炼宝、破釜灵印、剑走偏锋、引雷过载、噬宝成瘾。
   - 劫契 3 个：妖潮劫、精英劫、血月劫。
   - 暂缓：血祭灵匣、贪灵入骨、雷火劫、藏锋劫、灵宝劫。

3. 出现规则：
   - 契约不进入普通升级三选一。
   - 风险事件高概率出现邪契/劫契选择。
   - 小Boss后可出现邪契 / 稀有升级 / 高价值装备 三选一。
   - 第一精英后小概率出现轻邪契。
   - 每局契约选择总次数最多 2 次。
   - 已接受契约最多 2 个。
   - 劫契最多 1 个。
   - 玩家可放弃，放弃不惩罚。

4. 卡片字段：
   - 类型：邪契 / 劫契。
   - 名称。
   - 收益。
   - 代价。
   - 适合局势。
   - 局势提示。
   - 接受按钮。
   - 放弃按钮。

5. 局势提示：
   - 已有血葫护体时，提示可缓解生命代价。
   - 已有青锋追影时，提示暴击收益更高。
   - 已有雷域连锁时，提示雷击过载收益更高。
   - 当前生命上限偏低时，提示容错下降。
   - 没有吸血/回春时，提示燃魂代价风险高。
   - 装备技较少时，提示噬宝成瘾收益不稳定。

6. 战斗效果：
   - 断脉剑诀：飞剑/剑影伤害提高，最大生命降低。
   - 燃魂炼宝：法宝冷却降低，法宝触发消耗当前生命。
   - 破釜灵印：低血全伤害提高，生命无法自然恢复到 70% 以上。
   - 剑走偏锋：暴击率和暴击伤害提高，非暴击伤害降低。
   - 引雷过载：雷击弹跳和雷爆范围提高，周期生成反噬雷预警。
   - 噬宝成瘾：装备技伤害提高，装备技空窗时玩家伤害降低。
   - 妖潮劫：怪物数量和灵气掉落提高。
   - 精英劫：后续精英增加，装备品质提高，精英生命提高。
   - 血月劫：吸血/治疗提高，敌人伤害提高。

7. 表现与结算：
   - 接受邪契时显示 `邪契缔结：xxx` 和暗红/紫色脉冲。
   - 接受劫契时显示 `劫契降临：xxx` 和金红/黑金场景脉冲。
   - 构筑面板显示已接受契约和代价。
   - HUD 或构筑面板显示当前劫契环境变化。
   - 结算页展示契约收益贡献和代价影响。
   - 统计至少覆盖：消耗生命、反噬雷次数、额外怪物或灵气、装备技空窗惩罚次数、接受/放弃契约次数。

8. 商业化边界：
   - 不卖契约。
   - 不卖契约刷新。
   - 不卖契约免代价。
   - 不做付费专属契约。
   - 月卡/礼包不影响契约出现率。
   - 契约不影响局外资产，不造成永久负面。

阻塞影响：

如果没有邪契/劫契，灵契系统虽然增加了组合深度，但玩家中长期仍可能形成固定最优追求路线。契约系统用于让每局根据当前局势产生“敢不敢赌”的变化。

期望输出文件或结论：

- 更新 `src/data.js` 的契约配置。
- 更新 `game.js` 中契约出现、选择、放弃、效果、代价、表现和结算统计。
- 必要时更新 `styles.css` 和 UI 渲染。
- 更新 `smoke-test.js` 检查契约配置基础字段。
- 完成后运行 `npm test`。
- 建议测试剑修暴击局、雷修雷域局、回春/吸血局，观察不同局势下是否会改变契约接受判断。

状态：已完成

主程序进展：

- 已新增 `PACT_OPTIONS` 契约配置表，首版包含 6 个邪契、3 个劫契；每条包含 id、类型、名称、收益、代价、适合局势、效果、代价效果、入口和统计字段。
- 已将契约接入局内奖励队列：契约弹窗会暂停战斗，和升级/装备弹窗串联处理；玩家可接受或放弃，放弃无惩罚。
- 已实现出现规则首版：风险节点可触发契约，第一精英后小概率出现邪契，小Boss后必定出现契约选择；每局最多出现 2 次，最多接受 2 个，劫契最多 1 个。
- 已实现 9 个契约的首版战斗效果与代价：飞剑/剑影增伤与降生命、法宝冷却与燃魂扣血、低血增伤与治疗上限、暴击赌伤、雷击过载与反噬雷、装备技收益与空窗惩罚、妖潮额外怪与灵气、精英劫装备品质与精英压力、血月吸血治疗与敌伤提高。
- 已新增契约卡片 UI：展示类型、名称、收益、代价、适合局势、局势提示、接受按钮和放弃按钮。
- 已在构筑面板展示已接受契约与劫契环境，在结算页展示契约收益/代价和统计。
- 已更新 `smoke-test.js`，检查契约配置数量和基础字段完整性。

验证：

- `npm test` 已通过。
- 本地服务 `/health` 返回 ok，`/game.js` 返回 200。
