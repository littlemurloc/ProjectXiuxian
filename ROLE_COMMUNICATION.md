# 灵宝试炼 虚拟职务沟通文档

## 1. 职务总表

本项目只保留 3 个虚拟职务：

- 主策划
- UI/UX
- 主程序

其他工作，如数值、运营、商业化、美术需求、埋点方案，不单独设职务，由以上 3 个职务按职责分配处理。

## 2. 主策划

当前对话职务：**主策划 / 设计文案负责人**

### 工作职责

- 核心玩法方向与体验目标
- 职业、BD、装备、怪物、事件、关卡节奏设计
- 数值框架和首测指标
- 商业化边界、付费点、运营目标
- 世界观、命名、文本风格
- 美术需求的设计侧描述
- 设计文档维护
- 跨职务需求协调

### 主要负责文件

- `DESIGN.md`
- `CONTENT_SPEC.md`
- `PROJECT_CONVENTIONS.md`
- `ROLE_COMMUNICATION.md`
- 后续 `docs/design/` 下的设计文档

### 可读取文件

- 全部项目文件

### 写入边界

主策划可以写入设计与规范文档。

主策划不得在未获项目负责人明确同意时，直接修改：

- UI/UX 最终方案文档
- 程序架构文档
- `src/` 下的核心程序实现
- `index.html`
- `styles.css`
- `server.js`
- `smoke-test.js`

如主策划需要 UI/UX 或主程序支持，应在本文档的跨职务需求记录中提出。

## 3. UI/UX

### 工作职责

- 页面信息架构
- 战斗 HUD
- 背包、结算、商店/活动验证界面
- 交互流程
- 可读性与操作效率
- 原型视觉规范
- 美术表现需求整理，如图标、品质色、技能反馈、界面动效

### 主要负责文件

- 后续 `docs/ui/` 下的 UI/UX 文档
- UI 原型说明
- 界面流程图
- 样式规范文档

### 可读取文件

- `PROJECT_CONVENTIONS.md`
- `ROLE_COMMUNICATION.md`
- `DESIGN.md`
- `CONTENT_SPEC.md`
- `IMPLEMENTATION_PLAN.md`
- 程序文件中与界面结构直接相关的部分

### 写入边界

UI/UX 可以写入 UI/UX 相关文档。

UI/UX 不得在未获项目负责人明确同意时，直接修改：

- 核心玩法规则
- 职业、BD、装备、怪物、商业化边界
- 程序架构
- `src/` 下的核心程序实现

如 UI/UX 需要玩法设定补充，应向主策划提出需求。  
如 UI/UX 需要技术可行性判断，应向主程序提出需求。

## 4. 主程序

### 工作职责

- Web 原型技术架构
- 游戏循环
- 输入、战斗、碰撞、怪物 AI
- 数据驱动结构
- 装备、掉落、事件和结算的程序实现
- 本地存档与事件日志
- 测试脚本
- 本地服务器
- 将设计和 UI/UX 方案转化为可运行原型

### 主要负责文件

- `index.html`
- `styles.css`
- `server.js`
- `smoke-test.js`
- 后续 `src/` 下的程序文件
- 后续 `docs/technical/` 下的技术文档

### 可读取文件

- `PROJECT_CONVENTIONS.md`
- `ROLE_COMMUNICATION.md`
- `DESIGN.md`
- `CONTENT_SPEC.md`
- `IMPLEMENTATION_PLAN.md`
- UI/UX 相关文档

### 写入边界

主程序可以写入程序文件、测试文件和技术文档。

主程序不得在未获项目负责人明确同意时，直接修改：

- 核心玩法方向
- 职业、BD、装备、怪物、事件的设计定义
- 商业化边界
- UI/UX 最终方案

如主程序因设计缺口无法实现，应向主策划提出需求。  
如主程序因界面流程不清无法实现，应向 UI/UX 提出需求。

## 5. 跨职务沟通规则

当某职务 Agent 遇到阻塞时，应在本文档的“跨职务需求记录”中追加需求，不得擅自越权处理。

需求格式：

```md
### YYYY-MM-DD 发起职务 -> 目标职务

需求标题：

背景：

需要对方提供：

阻塞影响：

期望输出文件或结论：

状态：待处理 / 处理中 / 已完成
```

## 6. 跨职务需求记录

当前规则：

- 本节只保留待处理、处理中、待确认或最近一次关键交接。
- 已完成的历史沟通记录移至 `ROLE_COMMUNICATION_ARCHIVE.md`。
- 新需求仍按上方模板追加；完成后可在阶段整理时归档。

当前未完成需求：

### 2026-05-29 主策划 -> 主程序

需求标题：装备系统改为局内灵器商人与局外器谱解锁

背景：

项目负责人体验后确认：原“怪物掉装备 + 装备对比弹窗 + 局外装备养成/带入”的节奏会打断局内战斗，且长期规则不够干净。主策划已将装备系统改为“局内临时灵器 + 铸币 + 商人奇遇 + 局外器谱解锁”。本条需求覆盖旧装备掉落池、装备对比弹窗、局外装备库、强化/洗练/分解和开局穿戴逻辑，后续以新口径为准。

需要对方提供：

1. 移除旧装备流程：
   - 普通怪不再掉可穿戴装备。
   - 不再随机弹出装备替换/装备对比窗口。
   - 不做局外装备库、装备实例保留、开局穿戴、装备强化、装备洗练、装备分解。
   - 结算后本局灵器清空。

2. 实现局内灵器位：
   - 首版 3 个局内灵器位：兵器、灵佩、护符。
   - 每局从空灵器起手，每个灵器位最多装备 1 件。
   - 购买同位灵器时弹出替换确认，只影响本局。
   - 灵器卡需要显示名称、品质、灵器位、价格、标签、装备技、推荐语和当前构筑适配理由。

3. 实现铸币：
   - 怪物、精英、Boss、风险事件掉落铸币。
   - 铸币是本局临时货币，只用于商人购买、刷新或锁定商品。
   - 铸币结算后清空。
   - 不提供付费购买铸币入口。

4. 实现商人奇遇：
   - 商人不做 HUD 常驻商店，不自动弹窗。
   - 触发后在场景或 HUD 显示 `发现商人，按 E 交易`。
   - 玩家主动交互后暂停战斗并打开商店。
   - 触发节奏：第 1 次 80 杀或 1:45；第 2 次 220 杀或 4:00；第 3 次 420 杀或 6:30。
   - 商人入口建议保留 45 秒，或到下一个强事件前消失。

5. 实现商人类型与商品生成：
   - 兵铸师：职业装备池。
   - 闲云野鹤：法宝装备池。
   - 游方商人：流派装备池。
   - 杂货商人：通用补短板池。
   - 商人类型按当前职业、已装填法宝、主流派标签、已激活/接近灵契、空灵器位、生命压力加权。
   - 每局至少 1 次商人与当前职业或已装填法宝相关。
   - 第一次商人不出现黑市/高阶商人，连续两次不能都是杂货商人，未装填法宝前降低闲云野鹤权重。
   - 每个商店展示 4 件商品：1 件当前空位装备、1 件当前构筑适配装备、1 件低价过渡装备、1 件高价强力或惊喜装备。
   - 支持购买、消耗铸币刷新、锁定 1 件商品。

6. 实现器谱解锁与标签分池：
   - 怪物、精英、Boss、风险事件可掉落未解锁装备器谱。
   - 获得器谱后永久解锁对应装备，使其以后可进入局内商人池。
   - 器谱不是本局可穿戴装备，获得时提示 `获得器谱：xxx，已解锁，今后可在局内装备商店中刷新出现`。
   - 初始解锁 9-12 件基础装备，其余通过器谱解锁。
   - 装备不进入全局等权随机池，而按标签分池：slot、class、relic、archetype、function、merchant。
   - 首版装备总量目标仍约 30 件，最低可用 24 件；如内容不足，优先保证每个商人池有足够可抽样本。

7. 更新页面与结算：
   - 准备页展示器谱图鉴入口，不展示已穿戴 6 件装备。
   - 局外养成页改为器谱、法宝、职业三个分页。
   - 结算展示本局购买过的灵器、器谱解锁、铸币获得/花费和下一局建议。
   - 商店/验证页不再展示洗练礼包，保留月卡、首充礼包、法宝养成包、灵石补给包。

阻塞影响：

如果程序继续保留旧装备掉落弹窗，会继续打断局内节奏，并与新设计的商人奇遇、器谱解锁和临时灵器定位冲突。

期望输出文件或结论：

- 更新装备、掉落、商人、结算、局外养成和商店验证相关实现。
- 更新 `smoke-test.js` 检查：3 个灵器位、铸币、器谱、商人触发阈值、商人类型、商品 4 件、购买/刷新/锁定、旧装备库/强化/洗练/分解不再作为流程出现。
- 完成后运行 `npm test`，并建议用 8 分钟正式局或 4 分钟快测确认 2-3 次商人都能触发且不会随机打断战斗。

状态：已完成

主程序进展：

- 已将局内灵器位改为 3 槽：兵器、灵佩、护符；每局空槽起手，结算后本局灵器清空。
- 普通怪不再掉可穿戴装备，也不再随机弹出旧装备对比/替换窗口；旧候选装备区域改为商人状态提示。
- 已新增局内铸币：怪物、精英、Boss 可掉落，本局内用于商人购买、刷新、锁定，结算后清空且不接付费入口。
- 已新增灵器商人奇遇：80 杀或 1:45、220 杀或 4:00、420 杀或 6:30 触发；触发后提示“发现商人，按 E 交易”，玩家主动交互后暂停并打开商店。
- 已实现 4 类商人：兵铸师、闲云野鹤、游方商人、杂货商人，并按职业、法宝、主流派、空槽位和生命压力加权生成 4 件商品。
- 已实现商店购买、铸币刷新、锁定 1 件商品；购买同槽位灵器会弹出替换确认，替换只影响本局。
- 已新增器谱解锁：未解锁灵器会通过掉落解锁为永久器谱，提示“已解锁，今后可在局内装备商店中刷新出现”。
- 局外装备页已改为器谱图鉴，准备页显示器谱解锁进度，不再展示 6 件已穿戴装备。
- 结算已补充本局购买灵器、器谱解锁、铸币获得/花费/刷新/锁定/未用统计。
- 商店验证页已移除“洗练礼包”，改为“灵石补给包”，保留月卡、首充礼包、法宝养成包。
- 已更新 `smoke-test.js` 覆盖商人、铸币、器谱和新增 DOM。

验证：

- `npm test` 已通过。
- 本地服务已启动，`/health` 返回 ok，`/game.js` 返回 200，首页返回 200。

### 2026-05-27 主策划 -> 主程序

需求标题：局内表现与法宝装填节奏修正

背景：

项目负责人试玩法宝局内装填版本后，反馈了 4 个影响可读性和节奏的问题：剑修飞剑穿透提升不明显、法宝相关装备早于法宝出现时略显脱节、法宝装填时机偏晚、雷修弹射范围过大接近满屏。

需要对方提供：

1. 剑修飞剑表现：
   - 飞剑每命中 1 个敌人都显示伤害数字。
   - 飞剑达到当前穿透命中上限后，弹道立即消失。
   - 飞剑未打满穿透数量时，可继续飞到射程终点。
   - 获得穿透提升后，首次触发显示 `飞剑贯穿 +1` 或等价提示。

2. 法宝装填节奏：
   - 法宝装填改为击杀数阶梯触发。
   - 当前首测启用前 2 个法宝格：法宝 1 在 50 杀触发，法宝 2 在 150 杀触发。
   - 后续扩展阈值保留：法宝 3 在 300 杀触发，法宝 4 在 600 杀触发。
   - HUD 增加下一法宝装填进度条，格式如 `下一法宝 37 / 50`；装填后切换到下一档，例如 `下一法宝 82 / 150`。
   - 当前所有可用法宝格均已装填后，隐藏进度条或显示 `法宝已满`。

3. 法宝相关装备掉落：
   - 第一个法宝装填前，降低法宝专属装备和法宝专属词条掉落权重。
   - 第一个法宝装填前，装备推荐评分不因“未装填法宝适配”给高分。
   - 如果仍掉落法宝相关装备，装备卡提示：`适合法宝路线，装填相关法宝后价值提高`。
   - 第一个法宝装填后，提高与已装填法宝适配的装备和词条权重。

4. 雷系来源半径：
   - 雷系连锁范围读取“效果来源”，不读取职业身份。
   - 所有雷系跳跃、弹射、连锁、短电弧、追击效果，都必须有明确来源半径。
   - 默认从上一个命中目标周围搜索下一个目标，不允许全屏搜索。
   - 如果某个雷系效果没有单独配置半径，则继承触发它的上一级雷系效果半径，不能默认读取雷修普攻半径。
   - 雷修普攻“引雷击”：160-200 像素。
   - 引雷珠“珠引天雷”：200-240 像素，剑修、雷修或其他职业装填后都读取引雷珠自己的半径。
   - 引雷杖“余雷电弧”：120-160 像素。
   - 雷域连锁短电弧：120-160 像素，并限制在冰域内。
   - 灵宝灌雷额外分裂：继承余雷电弧半径。
   - 雷纹戒“雷纹追击”：不提供新半径，只在当前来源半径内改变目标优先级。
   - 引雷过载：只增加弹跳次数和雷爆范围，不默认扩大弹射搜索半径；若要扩大搜索半径需单独明写。

阻塞影响：

如果不修正，玩家难以看懂穿透提升，法宝装填目标感不足，法宝相关装备会显得与当前局势脱节，雷系连锁也容易变成无条件满屏弹射，削弱职业短板和组合价值。

期望输出文件或结论：

- 更新飞剑命中/销毁逻辑、伤害数字、雷系来源半径表、法宝装填触发配置、HUD 法宝进度条、装备掉落权重和装备卡提示。
- 更新 `smoke-test.js` 检查法宝装填阈值、HUD 文案和雷击范围配置。
- 完成后运行 `npm test`，并建议用剑修/雷修各跑一局 4 分钟快测。

状态：已完成

主程序进展：

- 已调整飞剑命中表现：飞剑每次命中都会在敌人位置显示伤害数字；当前穿透命中次数耗尽后弹道立即消失，未耗尽时继续飞到射程终点。
- 已增加穿透提升反馈：检测到飞剑穿透值高于本局已提示值后，首次触发显示 `飞剑贯穿 +1`。
- 已将法宝装填节奏改为击杀数阶梯：首测启用 2 格，法宝 1 在 50 杀触发，法宝 2 在 150 杀触发；配置中保留 300 杀、600 杀扩展阈值。
- 已在 HUD 法宝状态区域增加下一法宝装填进度，显示 `下一法宝 当前击杀 / 阈值`；法宝已满后显示 `法宝已满`。
- 已调整法宝相关装备掉落池：第一个法宝装填前降低法宝相关装备权重；装填后提高与已装填法宝标签适配的装备权重。
- 已调整装备推荐评分：第一个法宝装填前，不再因为未装填法宝标签适配获得高分；若仍出现法宝相关装备，会提示 `适合法宝路线，装填相关法宝后价值提高`。
- 已按“雷系来源半径”口径拆分范围：雷修普攻 `引雷击` 使用 180 像素，引雷珠 `珠引天雷` 使用 220 像素，引雷杖 `余雷电弧` 使用 140 像素，雷域连锁短电弧使用 140 像素并要求目标处于冰域/麻痹状态；灵宝灌雷额外分裂继承余雷电弧半径；雷纹戒只改变目标优先级，不新增搜索半径；引雷过载只增加弹跳次数和雷爆范围，不默认扩大搜索半径。
- 已更新 `smoke-test.js`，检查法宝装填阈值、HUD 文案、雷击范围配置、装备提示和飞剑贯穿提示。

验证：

- `npm test` 已通过。
- 本地服务 `/health` 返回 ok，`/game.js` 返回 200。

### 2026-05-27 主策划 -> 主程序

需求标题：法宝从开局携带改为局内装填三选一

后续修正说明：本条记录中的旧触发时机“90 杀 / 1:10 兜底、第一精英 / 3:30 兜底”已被后续“局内表现与法宝装填节奏修正”覆盖。当前最新口径为法宝 1 在 50 杀触发、法宝 2 在 150 杀触发，后续扩展阈值为 300 杀、600 杀。

背景：

项目负责人已审核通过法宝系统结构调整。法宝不再作为开局直接携带战力，而改为“局外解锁养成，局内逐步装填”的构筑模块。该改动用于提高同职业多局体验差异，减少固定开局配置导致的长期疲劳。

需要对方提供：

1. 准备页调整：
   - 移除“选择携带法宝”的开局流程。
   - 改为展示法宝库、法宝养成状态、1 个偏好法宝设置，以及“本局法宝将在试炼中逐步装填”的说明。

2. 局内装填流程：
   - 开局默认 2 个空法宝格，HUD 显示“等待装填”。
   - 第一次法宝装填：击杀 90 只怪，或 1:10 兜底触发。
   - 第二次法宝装填：击杀第一精英后触发，或 3:30 兜底触发。
   - 每次弹出 3 个法宝候选，候选来自局外已解锁法宝。
   - 第一次候选至少包含 1 个高养成或偏好法宝候选逻辑，但不保证指定法宝必出。
   - 已装填法宝不再进入本局候选。
   - 法宝三选一进入强决策奖励队列，弹窗期间暂停战斗。

3. 装填后规则：
   - 装填后法宝立即生效，HUD 显示法宝名、技能名、冷却/充能和触发状态。
   - 自动法宝进入冷却，被动法宝开始监听条件，主动法宝显示快捷入口。
   - 未装填聚妖铃前不显示聚妖铃主动快捷入口。
   - 未装填法宝的专属升级、法宝联动升级和相关灵契条件不得进入本局有效池。

4. 系统联动：
   - 升级池根据“已装填法宝”而非“已携带法宝”解锁法宝相关选项。
   - 灵契条件中的法宝条件统一读取“本局已装填法宝”。
   - 装备推荐评分、契约局势提示、结算统计统一读取“已装填法宝”。
   - 灵匣可以给已装填法宝强化，但首版不能直接增加额外法宝格。

5. 商业化边界：
   - 不实现付费指定法宝必出。
   - 不实现付费额外法宝格。
   - 不实现法宝候选付费刷新。
   - 不实现付费专属法宝或付费专属升星被动。

阻塞影响：

如果程序仍按“开局携带法宝”实现，后续升级池、灵契、邪契提示和长期复玩节奏都会与最新设计口径冲突。

期望输出文件或结论：

- 更新准备页、HUD、法宝三选一弹窗、升级池过滤、灵契条件、装备推荐、契约局势提示和结算统计。
- 必要时更新 `src/data.js` 的法宝候选权重、偏好法宝字段和法宝装填触发配置。
- 更新 `smoke-test.js` 检查法宝局内装填关键文本和状态。
- 完成后运行 `npm test`，并建议用 4 分钟快测确认两个装填节点均可触发。

状态：已完成

主程序进展：

- 已将 `equippedRelics` 的运行语义迁移为“本局已装填法宝”，开局默认清空，不再直接携带法宝战力。
- 已新增 `RELIC_LOAD_CONFIG`，配置 2 个装填槽、第一次装填击杀 90 只怪或 1:10 兜底、第二次装填第一精英或 3:30 兜底、每次 3 个候选。
- 已新增法宝装填三选一弹窗，接入强决策奖励队列，弹窗期间暂停战斗；候选来自局外法宝库，已装填法宝不会再次出现。
- 第一次装填候选会优先纳入偏好法宝或高养成法宝，但不保证指定法宝必出。
- 装填后法宝立即生效：自动法宝进入冷却，被动法宝开始监听，主动法宝在已装填后才可通过空格释放。
- 准备页改为展示法宝库、偏好法宝和“本局法宝将在试炼中逐步装填”的说明；局外法宝页按钮改为“设为偏好”，不再“带入/卸下”。
- HUD 法宝状态改为 2 个法宝格，未装填时显示“等待装填”，装填后显示法宝名、技能名、冷却/触发状态。
- 升级池、灵契条件、装备推荐、契约局势提示和灵匣法宝强化均读取“本局已装填法宝”；未装填任何法宝前，灵宝标签相关升级不会进入有效池。
- 灵匣仍可强化已装填法宝，但首版不会直接增加额外法宝格。
- 已更新 `smoke-test.js`，检查法宝局内装填配置、关键文本、弹窗 DOM 和实现标记。

验证：

- `npm test` 已通过。
- 本地服务 `/health` 返回 ok，`/game.js` 返回 200。

最近关键状态：

- 网页原型已完成流程拆分、基础战斗、升级、旧版法宝、装备、灵契、邪契/劫契、灵机等阶段性实现。
- 主策划已确认：法宝系统改为局外法宝库 + 局内装填三选一，不再开局携带法宝。
- 主策划已确认：灵机锁定升级属于“已预约但未选择”，本次升级界面不可再选，下次升级三选一中必出。
- 后续如继续推进，应优先围绕长期复玩疲劳、构筑变化、UI可读性和数值体验提出新的跨职务需求。


